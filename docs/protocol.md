# Hourly protocol

Read STATE + topics -> Research one topic -> Write memory/ artifact -> Update STATE -> Commit -> Email if a brief shipped.

Cadence: RRULE:FREQ=HOURLY
Repo: sutong-claude/EternalForge
Context store: this git tree. Sessions do not persist; files do.

Frozen: adding SearchAdapters / OpenAlex extras / group_by clones.
Required: one human-readable artifact under memory/ every hour.
