# Changelog

## 0.16.74 — 2026-09-19

- Fifty-eighth live SearchAdapter: OpenAlex works-by-has_oa_hybrid extras (`tools.oahasoahybrid`) grouping works via `group_by=has_oa_hybrid` with work counts, optional cites, and hybrid-OA-filtered OpenAlex URLs. Optional `OPENALEX_MAILTO`.
- Aliases `hasoahybrid-oa` / `works-hasoahybrid-oa`; `multi` includes oahasoahybrid. Public imports stay on `tools.research`.
- Tests cover empty query, aliases, group_by mapping (most works first), limit, and has_oa_hybrid-list shape.
- No PubMed heading invented.

## 0.16.73 — 2026-09-19

- Fifty-seventh live SearchAdapter: OpenAlex works-by-has_oa_gold extras (`tools.oahasogold`) grouping works via `group_by=has_oa_gold` with work counts, optional cites, and gold-OA-filtered OpenAlex URLs. Optional `OPENALEX_MAILTO`.
- Aliases `hasogold-oa` / `works-hasogold-oa`; `multi` includes oahasogold. Public imports stay on `tools.research`.
- Tests cover empty query, aliases, group_by mapping (most works first), limit, and has_oa_gold-list shape.
- No PubMed heading invented.

## 0.16.72 — 2026-09-19

- Fifty-sixth live SearchAdapter: OpenAlex works-by-has_oa_repository extras (`tools.oahasoarepo`) grouping works via `group_by=has_oa_repository` with work counts, optional cites, and OA-repository-filtered OpenAlex URLs. Optional `OPENALEX_MAILTO`.
- Aliases `hasoarepo-oa` / `works-hasoarepo-oa`; `multi` includes oahasoarepo. Public imports stay on `tools.research`.
- Tests cover empty query, aliases, group_by mapping (most works first), limit, and has_oa_repository-list shape.
- No PubMed heading invented.

## 0.16.71 — 2026-09-19

- Fifty-fifth live SearchAdapter: OpenAlex works-by-has_oa_submitted extras (`tools.oahasoasub`) grouping works via `group_by=has_oa_submitted` with work counts, optional cites, and OA-submitted-filtered OpenAlex URLs. Optional `OPENALEX_MAILTO`.
- Aliases `hasoasub-oa` / `works-hasoasub-oa`; `multi` includes oahasoasub. Public imports stay on `tools.research`.
- Tests cover empty query, aliases, group_by mapping (most works first), limit, and has_oa_submitted-list shape.
- No PubMed heading invented.

## 0.16.70 — 2026-09-19

- Fifty-fourth live SearchAdapter: OpenAlex works-by-has_oa_accepted_or_published extras (`tools.oahasoaap`) grouping works via `group_by=has_oa_accepted_or_published` with work counts, optional cites, and OA-accepted-or-published-filtered OpenAlex URLs. Optional `OPENALEX_MAILTO`.
- Aliases `hasoaap-oa` / `works-hasoaap-oa`; `multi` includes oahasoaap. Public imports stay on `tools.research`.
- Tests cover empty query, aliases, group_by mapping (most works first), limit, and has_oa_accepted_or_published-list shape.
- No PubMed heading invented.

## 0.16.69 — 2026-09-19

- Fifty-third live SearchAdapter: OpenAlex works-by-has_fulltext extras (`tools.oahasft`) grouping works via `group_by=has_fulltext` with work counts, optional cites, and fulltext-filtered OpenAlex URLs. Optional `OPENALEX_MAILTO`.
- Aliases `hasft-oa` / `works-hasft-oa`; `multi` includes oahasft. Public imports stay on `tools.research`.
- Tests cover empty query, aliases, group_by mapping (most works first), limit, and has_fulltext-list shape.
- No PubMed heading invented.

## 0.16.68 — 2026-09-19

- Fifty-second live SearchAdapter: OpenAlex works-by-has_pdf extras (`tools.oahaspdf`) grouping works via `group_by=has_pdf` with work counts, optional cites, and PDF-filtered OpenAlex URLs. Optional `OPENALEX_MAILTO`.
- Aliases `haspdf-oa` / `works-haspdf-oa`; `multi` includes oahaspdf. Public imports stay on `tools.research`.
- Tests cover empty query, aliases, group_by mapping (most works first), limit, and has_pdf-list shape.
- No PubMed heading invented.

## 0.16.67 — 2026-09-19

- Fifty-first live SearchAdapter: OpenAlex works-by-has_abstract extras (`tools.oahasabs`) grouping works via `group_by=has_abstract` with work counts, optional cites, and abstract-filtered OpenAlex URLs. Optional `OPENALEX_MAILTO`.
- Aliases `hasabs-oa` / `works-hasabs-oa`; `multi` includes oahasabs. Public imports stay on `tools.research`.
- Tests cover empty query, aliases, group_by mapping (most works first), limit, and has_abstract-list shape.
- No PubMed heading invented.

## 0.16.66 — 2026-09-19

- Fiftieth live SearchAdapter: OpenAlex works-by-has_references extras (`tools.oahasrefs`) grouping works via `group_by=has_references` with work counts, optional cites, and reference-filtered OpenAlex URLs. Optional `OPENALEX_MAILTO`.
- Aliases `hasrefs-oa` / `works-hasrefs-oa`; `multi` includes oahasrefs. Public imports stay on `tools.research`.
- Tests cover empty query, aliases, group_by mapping (most works first), limit, and has_references-list shape.
- No PubMed heading invented.
